# nginx.default

server {
    listen 5001;
    server_name converter.ru;

    location / {
        proxy_pass web:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    location /static {
        root /opt/app/martor_demo;
    }
}
